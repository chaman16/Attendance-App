<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendenceapp">

    <application
        android:name=".ApplicationContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ViewAttendanceByFacultyActivit"></activity>
        <activity android:name=".AddAttendanceActivity" />
        <activity android:name=".AddAttandanceSessionActivity" />
        <activity android:name=".ViewStudentByBranchYear" />
        <activity android:name=".ViewStudentActivity" />
        <activity android:name=".ViewFacultyActivity" />
        <activity android:name=".AttendancePerStudentActivity" />
        <activity android:name=".AddFacultyActivity" />
        <activity android:name=".AddStudentActivity" />
        <activity android:name=".db.DBAdapter" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>